# It's better to keep this .env and the environment variables from the docker-compose in aws secrets manager or doppler.
# For this demo we can use .env file.
SECRET_KEY="your_secret_key" # Generate a random secret key using scripts/generate_secret.py
MONGO_URI="mongodb://localhost:27017/your_db_name" # Replace with your MongoDB URI
API_KEY="your_api_key" # Replace with your API key in case you want to use it instead JWT